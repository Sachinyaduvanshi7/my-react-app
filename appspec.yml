version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/react-app
hooks:
  Install:
    - location: install.sh
      timeout: 300
  AfterInstall:
    - location: afterinstall.sh
      timeout: 300
  ApplicationStart:
    - location: applicationstart.sh
